## Overview

You need map the [C++ template repository](https://github.com/innerNULL/cpp-cmake-conan-template) 
to a Rust version.

## Tips

- You need follows some Rust convensions. E.g., replace `src` with `crates`.

## Tasks

### 2026-09-17 20:00

Finish the initial version. It's better to have a `ndarray` + `numpy` for Python binder example.
